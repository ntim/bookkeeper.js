	<form action="/expense/update/<%=expense.id%>" method="POST" class="edit">
	<div class="form-group">
		<h2> editing a expense... </h2>
		<% if(flash && flash.err) { %>
<ul class="alert alert-success">
<% Object.keys(flash.err).forEach(function(error) { %>
<li><%- JSON.stringify(flash.err[error]) %></li>
<% }) %>
</ul>
<% } %>
		
		</div>
		<input type="hidden" name="_csrf" value="<%= _csrf %>" />
		<input type="submit" value="Update Expense" class="btn btn-default"/>
		<a href="/expense/show/<%=expense.id %>" class="btn btn-danger">Cancel</a>
	</form>
	
	